# 文件连接器 - 数据结构设计

**版本**: 1.0.0  
**更新时间**: 2026-02-01

---

## 1. 核心数据类型

### 1.1 卡片信息

```typescript
/**
 * 卡片基本信息
 */
interface CardInfo {
  /** 卡片唯一标识 */
  id: string;
  
  /** 卡片名称 */
  name: string;
  
  /** 卡片类型 */
  type: CardType;
  
  /** 卡片文件路径 */
  path: string;
  
  /** 卡片描述 */
  description?: string;
  
  /** 基础卡片信息 */
  baseCards: BaseCardInfo[];
  
  /** 当前资源模式 */
  resourceMode: ResourceMode;
  
  /** 元数据 */
  metadata: CardMetadata;
}

/**
 * 卡片类型
 */
type CardType = 
  | 'video'
  | 'audio'
  | 'image'
  | 'document'
  | 'text'
  | 'custom';

/**
 * 资源模式
 */
type ResourceMode = 
  | 'full'      // 全填充：资源在卡片内部
  | 'empty'     // 空壳：资源在外部
  | 'semi';     // 半空壳：部分在内部

/**
 * 基础卡片信息
 */
interface BaseCardInfo {
  /** 基础卡片ID（在卡片内的ID） */
  id: string;
  
  /** 基础卡片插件类型 */
  pluginType: string;
  
  /** 资源字段列表 */
  resourceFields: ResourceField[];
}

/**
 * 资源字段
 */
interface ResourceField {
  /** 字段名称 */
  name: string;
  
  /** 字段类型 */
  type: 'file' | 'url' | 'content';
  
  /** 当前值 */
  value?: string;
  
  /** 是否必需 */
  required: boolean;
  
  /** 允许的文件类型 */
  allowedTypes?: string[];
}

/**
 * 卡片元数据
 */
interface CardMetadata {
  /** 创建时间 */
  created_at: string;
  
  /** 修改时间 */
  modified_at: string;
  
  /** 版本 */
  version: string;
  
  /** 标签 */
  tags: string[];
}
```

### 1.2 文件信息

```typescript
/**
 * 文件基本信息
 */
interface FileInfo {
  /** 文件名（不含路径） */
  name: string;
  
  /** 完整路径 */
  path: string;
  
  /** 文件大小（字节） */
  size: number;
  
  /** MIME类型 */
  mimeType: string;
  
  /** 文件类型分类 */
  type: FileType;
  
  /** 扩展名 */
  extension: string;
  
  /** 修改时间 */
  modifiedAt: string;
  
  /** 创建时间 */
  createdAt: string;
}

/**
 * 文件类型
 */
type FileType = 
  | 'video'
  | 'audio'
  | 'image'
  | 'document'
  | 'subtitle'
  | 'archive'
  | 'other';

/**
 * 文件类型映射
 */
const FILE_TYPE_MAP: Record<string, FileType> = {
  // 视频
  'mp4': 'video',
  'mkv': 'video',
  'avi': 'video',
  'mov': 'video',
  'webm': 'video',
  'flv': 'video',
  
  // 音频
  'mp3': 'audio',
  'wav': 'audio',
  'flac': 'audio',
  'aac': 'audio',
  'ogg': 'audio',
  'm4a': 'audio',
  
  // 图片
  'jpg': 'image',
  'jpeg': 'image',
  'png': 'image',
  'gif': 'image',
  'webp': 'image',
  'svg': 'image',
  'bmp': 'image',
  
  // 文档
  'pdf': 'document',
  'doc': 'document',
  'docx': 'document',
  'xls': 'document',
  'xlsx': 'document',
  'ppt': 'document',
  'pptx': 'document',
  
  // 字幕
  'srt': 'subtitle',
  'ass': 'subtitle',
  'ssa': 'subtitle',
  'vtt': 'subtitle',
  
  // 压缩包
  'zip': 'archive',
  'rar': 'archive',
  '7z': 'archive',
  'tar': 'archive',
  'gz': 'archive'
};
```

### 1.3 绑定信息

```typescript
/**
 * 绑定项
 */
interface BindingItem {
  /** 卡片ID */
  cardId: string;
  
  /** 基础卡片ID（可选，用于多基础卡片场景） */
  baseCardId?: string;
  
  /** 资源字段名 */
  resourceField: string;
  
  /** 文件路径 */
  filePath: string;
  
  /** 绑定状态 */
  status?: BindingStatus;
  
  /** 额外信息 */
  extra?: Record<string, any>;
}

/**
 * 绑定状态
 */
type BindingStatus = 
  | 'pending'    // 待执行
  | 'executing'  // 执行中
  | 'success'    // 成功
  | 'failed'     // 失败
  | 'skipped';   // 已跳过

/**
 * 绑定详细信息（用于预览和报告）
 */
interface BindingDetail extends BindingItem {
  /** 卡片名称 */
  cardName: string;
  
  /** 文件名称 */
  fileName: string;
  
  /** 文件大小 */
  fileSize: number;
  
  /** 是否为新绑定 */
  isNew: boolean;
  
  /** 原有绑定（如果是修改） */
  previousPath?: string;
  
  /** 置信度（自动匹配时） */
  confidence?: number;
  
  /** 匹配原因（AI匹配时） */
  matchReason?: string;
}
```

---

## 2. 匹配相关类型

### 2.1 匹配方案

```typescript
/**
 * 匹配模式
 */
type MatchMode = 
  | 'number_sequence'  // 数字序列匹配
  | 'keyword'          // 关键词匹配
  | 'file_order';      // 文件顺序匹配

/**
 * 匹配方案
 */
interface MatchScheme {
  /** 匹配模式 */
  mode: MatchMode;
  
  /** 置信度 (0-100) */
  confidence: number;
  
  /** 绑定列表 */
  bindings: BindingItem[];
  
  /** 未匹配的卡片 */
  unmatchedCards: string[];
  
  /** 未匹配的文件 */
  unmatchedFiles: string[];
  
  /** 冲突信息 */
  conflicts?: MatchConflict[];
  
  /** 警告信息 */
  warning?: string;
  
  /** 方案描述 */
  description?: string;
}

/**
 * 匹配冲突
 */
interface MatchConflict {
  /** 冲突类型 */
  type: 'duplicate_card' | 'duplicate_file' | 'multiple_match';
  
  /** 相关卡片 */
  cards: string[];
  
  /** 相关文件 */
  files: string[];
  
  /** 冲突原因 */
  reason: string;
}
```

### 2.2 智能匹配结果

```typescript
/**
 * 智能匹配结果
 */
interface SmartMatchResult {
  /** 匹配项（包含置信度） */
  bindings: Array<BindingItem & {
    confidence: number;
    reason?: string;
  }>;
  
  /** 未匹配的卡片 */
  unmatchedCards: string[];
  
  /** 未匹配的文件 */
  unmatchedFiles: string[];
  
  /** 处理元信息 */
  metadata: {
    processingTime: number;
    modelVersion: string;
  };
}

/**
 * 置信度分级结果
 */
interface ClassifiedMatches {
  /** 高置信度 (≥80%) */
  high: Array<BindingItem & { confidence: number }>;
  
  /** 中置信度 (50-79%) */
  medium: Array<BindingItem & { confidence: number }>;
  
  /** 低置信度 (<50%) */
  low: Array<BindingItem & { confidence: number }>;
}
```

---

## 3. 执行相关类型

### 3.1 执行选项

```typescript
/**
 * 执行选项
 */
interface ExecuteOptions {
  /** 是否复制文件到卡片内部 */
  copyToInternal?: boolean;
  
  /** 错误处理策略 */
  errorStrategy?: 'skip' | 'retry' | 'abort';
  
  /** 最大重试次数 */
  maxRetries?: number;
  
  /** 并发数 */
  concurrency?: number;
  
  /** 进度回调 */
  onProgress?: (progress: ExecutionProgress) => void;
  
  /** 错误回调 */
  onError?: (error: ExecutionError) => ErrorAction;
}

/**
 * 错误处理动作
 */
type ErrorAction = 'skip' | 'retry' | 'abort';

/**
 * 执行进度
 */
interface ExecutionProgress {
  /** 当前索引 */
  current: number;
  
  /** 总数 */
  total: number;
  
  /** 当前绑定 */
  currentBinding?: BindingItem;
  
  /** 已完成数 */
  completed: number;
  
  /** 失败数 */
  failed: number;
  
  /** 跳过数 */
  skipped: number;
  
  /** 百分比 */
  percentage: number;
  
  /** 预计剩余时间（毫秒） */
  estimatedRemaining?: number;
}
```

### 3.2 执行结果

```typescript
/**
 * 执行结果
 */
interface ExecuteResult {
  /** 成功数量 */
  success: number;
  
  /** 失败数量 */
  failed: number;
  
  /** 跳过数量 */
  skipped: number;
  
  /** 总耗时（毫秒） */
  duration: number;
  
  /** 详细结果 */
  details: ExecutionDetail[];
  
  /** 错误列表 */
  errors: ExecutionError[];
}

/**
 * 执行详情
 */
interface ExecutionDetail {
  /** 绑定项 */
  binding: BindingItem;
  
  /** 状态 */
  status: 'success' | 'failed' | 'skipped';
  
  /** 耗时（毫秒） */
  duration: number;
  
  /** 错误信息（如果失败） */
  error?: string;
}

/**
 * 执行错误
 */
interface ExecutionError {
  /** 绑定项 */
  binding: BindingItem;
  
  /** 错误码 */
  code: string;
  
  /** 错误消息 */
  message: string;
  
  /** 错误栈 */
  stack?: string;
  
  /** 是否可重试 */
  retryable: boolean;
}
```

---

## 4. 转换相关类型

### 4.1 转换选项

```typescript
/**
 * 转换选项
 */
interface ConvertOptions {
  /** 目标路径（空壳模式） */
  targetPath?: string;
  
  /** 资源类型过滤 */
  resourceTypes?: FileType[];
  
  /** 大小限制（字节） */
  sizeLimit?: number;
  
  /** 冲突处理 */
  conflictStrategy?: 'overwrite' | 'rename' | 'skip';
  
  /** 进度回调 */
  onProgress?: (progress: ConvertProgress) => void;
}

/**
 * 转换进度
 */
interface ConvertProgress {
  /** 当前卡片索引 */
  currentCard: number;
  
  /** 总卡片数 */
  totalCards: number;
  
  /** 当前资源索引（当前卡片内） */
  currentResource: number;
  
  /** 当前卡片资源总数 */
  totalResources: number;
  
  /** 已传输字节数 */
  bytesTransferred: number;
  
  /** 总字节数 */
  totalBytes: number;
  
  /** 百分比 */
  percentage: number;
}
```

### 4.2 转换结果

```typescript
/**
 * 转换结果
 */
interface ConvertResult {
  /** 成功数量 */
  success: number;
  
  /** 失败数量 */
  failed: number;
  
  /** 总转换大小（字节） */
  totalSize: number;
  
  /** 总耗时（毫秒） */
  duration: number;
  
  /** 详细结果 */
  details: ConvertDetail[];
  
  /** 错误列表 */
  errors: ConvertError[];
}

/**
 * 转换详情
 */
interface ConvertDetail {
  /** 卡片ID */
  cardId: string;
  
  /** 卡片名称 */
  cardName: string;
  
  /** 状态 */
  status: 'success' | 'failed' | 'partial';
  
  /** 转换的资源数 */
  resourceCount: number;
  
  /** 转换的大小 */
  size: number;
}

/**
 * 转换错误
 */
interface ConvertError {
  /** 卡片ID */
  cardId: string;
  
  /** 资源路径 */
  resourcePath?: string;
  
  /** 错误码 */
  code: string;
  
  /** 错误消息 */
  message: string;
}
```

---

## 5. 配置类型

### 5.1 连接器配置

```typescript
/**
 * 文件连接器配置
 */
interface FileConnectorConfig {
  /** SDK实例 */
  sdk: ChipsSDK;
  
  /** AI服务配置 */
  aiService?: AIServiceConfig;
  
  /** 日志配置 */
  logger?: LoggerConfig;
  
  /** 匹配配置 */
  matching?: MatchingConfig;
  
  /** UI配置 */
  ui?: UIConfig;
}

/**
 * AI服务配置
 */
interface AIServiceConfig {
  /** 服务端点 */
  endpoint: string;
  
  /** 认证令牌 */
  token: string;
  
  /** 超时（毫秒） */
  timeout: number;
  
  /** 重试配置 */
  retry: {
    maxAttempts: number;
    delay: number;
  };
}

/**
 * 匹配配置
 */
interface MatchingConfig {
  /** 数字匹配配置 */
  number: {
    cardPosition: 'first' | 'last' | 'longest' | 'auto';
    filePosition: 'first' | 'last' | 'longest' | 'auto';
    conflictStrategy: 'first' | 'manual' | 'skip';
  };
  
  /** 关键词匹配配置 */
  keyword: {
    minSimilarity: number;
    ignoreWords: string[];
  };
  
  /** 顺序匹配配置 */
  order: {
    cardSort: 'name' | 'name_numeric' | 'created';
    fileSort: 'name' | 'name_numeric' | 'modified';
  };
}

/**
 * UI配置
 */
interface UIConfig {
  /** 默认连接模式 */
  defaultMode: 'manual' | 'auto' | 'smart';
  
  /** 默认视图 */
  defaultView: 'table' | 'graph';
  
  /** 每页显示数量 */
  pageSize: number;
  
  /** 是否显示预览 */
  showPreview: boolean;
}
```

---

## 6. 状态类型

### 6.1 Store状态

```typescript
/**
 * 连接器状态
 */
interface ConnectorState {
  /** 当前模式 */
  mode: 'manual' | 'auto' | 'smart' | 'convert';
  
  /** 当前视图 */
  view: 'table' | 'graph' | 'scheme' | 'preview' | 'execute';
  
  /** 选中的卡片 */
  selectedCards: CardInfo[];
  
  /** 选中的文件 */
  selectedFiles: FileInfo[];
  
  /** 当前绑定列表 */
  bindings: BindingItem[];
  
  /** 匹配方案列表 */
  schemes: MatchScheme[];
  
  /** 当前选中的方案索引 */
  selectedSchemeIndex: number;
  
  /** 执行状态 */
  executing: boolean;
  
  /** 执行进度 */
  progress: ExecutionProgress | null;
  
  /** 最后执行结果 */
  lastResult: ExecuteResult | null;
  
  /** 错误信息 */
  error: string | null;
  
  /** 加载状态 */
  loading: {
    cards: boolean;
    files: boolean;
    matching: boolean;
  };
}
```

### 6.2 视图状态

```typescript
/**
 * 表格视图状态
 */
interface TableViewState {
  /** 排序字段 */
  sortBy: 'cardName' | 'status' | 'fileName';
  
  /** 排序方向 */
  sortOrder: 'asc' | 'desc';
  
  /** 筛选条件 */
  filter: 'all' | 'bound' | 'unbound';
  
  /** 选中的行 */
  selectedRows: string[];
  
  /** 当前页 */
  currentPage: number;
  
  /** 每页数量 */
  pageSize: number;
}

/**
 * 图形视图状态
 */
interface GraphViewState {
  /** 缩放比例 */
  zoom: number;
  
  /** 画布偏移X */
  panX: number;
  
  /** 画布偏移Y */
  panY: number;
  
  /** 选中的连线 */
  selectedConnection: string | null;
  
  /** 卡片位置 */
  cardPositions: Record<string, Position>;
  
  /** 文件位置 */
  filePositions: Record<string, Position>;
  
  /** 是否正在拖拽 */
  dragging: boolean;
  
  /** 拖拽起点 */
  dragStart: Position | null;
}

/**
 * 位置
 */
interface Position {
  x: number;
  y: number;
}
```

---

**文档维护者**: 薯片生态核心团队
