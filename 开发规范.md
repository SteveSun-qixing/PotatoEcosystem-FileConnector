# 文件连接器 - 开发规范

**版本**: 1.0.0  
**更新时间**: 2026-01-31  
**基于**: [生态通用开发规范](../生态共用/08-开发规范总则.md)

---

## 1. 概述

本文档定义了文件连接器（Chips-FileConnector）的开发规范，是[生态通用开发规范](../生态共用/08-开发规范总则.md)的具体应用和补充。

### 1.1 强制性要求

本文档中的所有规范都是**强制性**的。不遵循规范的代码将无法通过审核。

### 1.2 规范优先级

1. **本文档规范** - 针对文件连接器的特定规范
2. **生态通用规范** - 适用于所有薯片生态项目的规范
3. **代码风格指南** - 编码风格细则

---

## 2. 架构规范 ⭐ 最重要

### 2.1 中心路由原则

**强制要求**：所有模块通信必须通过薯片内核（Chips Core），禁止模块间直接调用。

#### 2.1.1 正确示例

```typescript
// ✅ 正确：通过 Core 路由调用 FileIdentifier
const response = await Core.request({
  target: "FileIdentifier",
  action: "identify",
  params: { filePath: "/path/to/file.mp4" }
});

if (response.success) {
  const mimeType = response.data.mimeType;
  // 处理结果
}
```

#### 2.1.2 错误示例

```typescript
// ❌ 错误：直接导入和调用
import { FileIdentifier } from '@chips/foundation';
const mimeType = FileIdentifier.identify(filePath); // 禁止！

// ❌ 错误：直接引用其他模块
import { ConfigManager } from '../other-module';
ConfigManager.load(); // 禁止！
```

### 2.2 模块独立性

**原则**：每个模块必须独立，通过接口对外暴露功能。

```typescript
// ✅ 正确：定义接口
export interface IMatcher {
  match(cards: Card[], files: FileInfo[]): Promise<MatchingResult>;
  getInfo(): MatcherInfo;
}

// ✅ 正确：实现接口
export class NumberMatcher implements IMatcher {
  async match(cards: Card[], files: FileInfo[]): Promise<MatchingResult> {
    // 实现
  }
  
  getInfo(): MatcherInfo {
    return {
      name: 'NumberMatcher',
      description: '数字序列匹配器'
    };
  }
}
```

### 2.3 前后端分离

**强制要求**：
- ✅ 前端只负责 UI 展示和用户交互
- ✅ 后端只负责业务逻辑和数据处理
- ❌ 前端禁止包含业务逻辑
- ❌ 后端禁止包含 UI 代码

```typescript
// ❌ 错误：前端包含业务逻辑
// frontend/views/TableView.vue
const analyzeMatching = () => {
  // 复杂的匹配算法... 这应该在后端！
};

// ✅ 正确：前端调用后端服务
// frontend/views/TableView.vue
const analyzeMatching = async () => {
  const result = await MatchingService.analyze(cards, files);
  // 只处理 UI 更新
  updateUI(result);
};
```

---

## 3. 多语言支持规范 ⭐ 核心规范

### 3.1 零硬编码原则

**绝对禁止**在代码中硬编码任何文本内容。

```typescript
// ❌ 错误：硬编码文本
<button>确定</button>
alert("绑定成功");
console.log("开始处理文件");
throw new Error("文件不存在");

// ✅ 正确：使用多语言函数
<button>{t('common.ok')}</button>
alert(t('fileConnector.bindingSuccess'));
console.log(t('fileConnector.log.processingFile'));
throw new Error(t('fileConnector.error.fileNotFound'));
```

### 3.2 词汇表管理

**开发阶段词汇表** (`dev_i18n.yaml`):

```yaml
fileConnector:
  title: "文件连接器"
  
  mode:
    manual: "手动连接"
    auto: "自动识别"
    ai: "智能识别"
  
  action:
    selectCard: "选择卡片"
    selectFile: "选择文件"
    bind: "绑定"
    execute: "执行绑定"
    cancel: "取消"
  
  status:
    matched: "已匹配"
    unmatched: "未匹配"
    binding: "绑定中"
    success: "成功"
    failed: "失败"
  
  error:
    fileNotFound: "文件不存在"
    permissionDenied: "权限不足"
    diskFull: "磁盘空间不足"
    configCorrupted: "配置文件损坏"
  
  message:
    bindingSuccess: "绑定成功"
    bindingFailed: "绑定失败"
    processingFile: "正在处理文件"
```

### 3.3 打包时自动替换

开发时使用的 key（如 `fileConnector.title`）在打包时自动替换为系统词汇编码（如 `i18n.fileConnector.001`）。

---

## 4. 代码规范

### 4.1 命名规范

#### 4.1.1 通用命名

| 类型 | 规则 | 示例 |
|------|------|------|
| 类名 | PascalCase | `MatchingEngine`, `BindingExecutor` |
| 接口名 | PascalCase + I前缀 | `IMatcher`, `IView` |
| 函数/方法 | camelCase | `analyzeMatching`, `executeBinding` |
| 变量 | camelCase | `cardList`, `fileInfo` |
| 常量 | UPPER_SNAKE_CASE | `MAX_CONCURRENT`, `DEFAULT_TIMEOUT` |
| 文件名 | kebab-case | `matching-engine.ts`, `binding-executor.ts` |

#### 4.1.2 特定命名

**匹配器命名**：
- 类名：`{功能}Matcher`，如 `NumberMatcher`
- 文件名：`{功能}-matcher.ts`，如 `number-matcher.ts`

**视图命名**：
- 类名：`{名称}View`，如 `TableView`
- 文件名：`{名称}View.vue`，如 `TableView.vue`

### 4.2 注释规范

#### 4.2.1 文件头注释

```typescript
/**
 * 文件连接器 - 匹配引擎
 * 
 * 负责分析卡片和文件的命名规律，提供多种匹配算法。
 * 
 * @module matching-engine
 * @author Chips Team
 * @since 1.0.0
 */
```

#### 4.2.2 函数注释

```typescript
/**
 * 执行文件绑定操作
 * 
 * @param bindings - 绑定关系列表
 * @param options - 绑定选项
 * @returns 绑定结果，包含成功和失败的项目
 * @throws {FileConnectorError} 当磁盘空间不足时抛出 E_DISK_001
 * 
 * @example
 * ```typescript
 * const result = await executor.execute(bindings, {
 *   mode: 'full',
 *   pathType: 'relative'
 * });
 * console.log(`成功: ${result.statistics.success}`);
 * ```
 */
async execute(
  bindings: Binding[],
  options: BindingOptions
): Promise<BindingResult> {
  // 实现
}
```

### 4.3 错误处理

#### 4.3.1 错误代码规范

```typescript
// 错误代码格式：E_{分类}_{编号}
enum ErrorCode {
  // 文件错误 (E_FILE_xxx)
  FILE_NOT_FOUND = 'E_FILE_001',
  FILE_ACCESS_DENIED = 'E_FILE_002',
  FILE_TOO_LARGE = 'E_FILE_003',
  
  // 权限错误 (E_PERM_xxx)
  PERMISSION_DENIED = 'E_PERM_001',
  
  // 磁盘错误 (E_DISK_xxx)
  DISK_FULL = 'E_DISK_001',
  
  // 配置错误 (E_CONFIG_xxx)
  CONFIG_CORRUPTED = 'E_CONFIG_001',
  
  // 网络错误 (E_NET_xxx)
  NETWORK_ERROR = 'E_NET_001',
  API_TIMEOUT = 'E_NET_002',
}
```

#### 4.3.2 错误类使用

```typescript
// ✅ 正确：使用自定义错误类
throw new FileConnectorError(
  ErrorCode.FILE_NOT_FOUND,
  t('fileConnector.error.fileNotFound'),
  { filePath: '/path/to/file.mp4' }
);

// ✅ 正确：捕获和处理错误
try {
  await operation();
} catch (error) {
  if (error instanceof FileConnectorError) {
    Logger.error(`Error ${error.code}: ${error.message}`, error);
    // 显示友好的错误提示
    UI.showError(error.message);
  } else {
    Logger.error('Unknown error', error);
    UI.showError(t('error.unknown'));
  }
}
```

---

## 5. 公共基础层使用规范

### 5.1 必须使用的基础层模块

文件连接器**必须**使用以下公共基础层模块：

| 模块 | 用途 | 调用方式 |
|------|------|---------|
| FileIdentifier | 识别文件类型 | `Core.request({ target: "FileIdentifier", action: "identify", params: {...} })` |
| ConfigManager | 读写配置文件 | `Core.request({ target: "ConfigManager", action: "read/update", params: {...} })` |
| LogSystem | 记录日志 | `Core.request({ target: "LogSystem", action: "log", params: {...} })` |
| DataSerializer | 序列化 YAML/JSON | `Core.request({ target: "DataSerializer", action: "parse/stringify", params: {...} })` |

### 5.2 文件识别规范

```typescript
// ✅ 正确：使用 FileIdentifier
async function identifyFile(filePath: string): Promise<string> {
  const response = await Core.request({
    target: "FileIdentifier",
    action: "identify",
    params: { filePath }
  });
  
  if (!response.success) {
    throw new FileConnectorError(
      ErrorCode.FILE_IDENTIFICATION_FAILED,
      t('fileConnector.error.identificationFailed'),
      { filePath, error: response.error }
    );
  }
  
  return response.data.mimeType;
}

// ❌ 错误：自己实现文件识别
function identifyFile(filePath: string): string {
  const ext = path.extname(filePath);
  // 不要重复实现文件识别逻辑！
  return mimeTypes[ext];
}
```

### 5.3 配置管理规范

```typescript
// ✅ 正确：使用 ConfigManager
async function updateCardConfig(
  cardId: string,
  updates: Partial<CardConfig>
): Promise<void> {
  const response = await Core.request({
    target: "ConfigManager",
    action: "update",
    params: {
      path: `cards/${cardId}/metadata.yaml`,
      data: updates
    }
  });
  
  if (!response.success) {
    throw new FileConnectorError(
      ErrorCode.CONFIG_UPDATE_FAILED,
      t('fileConnector.error.configUpdateFailed'),
      { cardId, error: response.error }
    );
  }
}
```

### 5.4 日志记录规范

```typescript
// ✅ 正确：使用 LogSystem
async function logOperation(
  level: LogLevel,
  message: string,
  context?: any
): Promise<void> {
  await Core.request({
    target: "LogSystem",
    action: "log",
    params: {
      level,
      message: t(message), // 使用多语言
      context,
      module: "FileConnector"
    }
  });
}

// 使用示例
await logOperation('info', 'fileConnector.log.bindingStart', {
  cardCount: 10,
  fileCount: 10
});
```

---

## 6. 性能规范

### 6.1 性能指标

**强制要求**：

| 操作 | 目标时间 |
|------|---------|
| 界面打开 | < 500ms |
| 切换模式 | < 100ms |
| 自动识别（100 个文件） | < 2s |
| 智能识别（100 个文件） | < 5s |
| 绑定执行（100 个小文件） | < 2min |

### 6.2 并发控制

```typescript
// ✅ 正确：限制并发数
class BatchProcessor {
  private concurrency: number = 5; // 最大并发数
  
  async processBatch<T, R>(
    items: T[],
    processor: (item: T) => Promise<R>
  ): Promise<R[]> {
    const queue = new PromiseQueue({
      concurrency: this.concurrency
    });
    
    for (const item of items) {
      queue.add(() => processor(item));
    }
    
    return await queue.onIdle();
  }
}

// ❌ 错误：无限并发
const results = await Promise.all(
  items.map(item => processor(item)) // 可能导致系统卡顿
);
```

### 6.3 缓存策略

```typescript
// ✅ 正确：使用缓存
class MatchingEngine {
  private cache = new CacheManager();
  
  async analyze(cards: Card[], files: FileInfo[]): Promise<MatchingResult> {
    const cacheKey = this.getCacheKey(cards, files);
    
    // 尝试从缓存读取
    const cached = this.cache.get(cacheKey);
    if (cached) {
      return cached;
    }
    
    // 执行分析
    const result = await this.doAnalyze(cards, files);
    
    // 存入缓存
    this.cache.set(cacheKey, result, 3600); // 1小时过期
    
    return result;
  }
}
```

---

## 7. 测试规范

### 7.1 测试覆盖率

**强制要求**：
- 核心模块：≥ 90%
- 业务模块：≥ 80%
- UI 组件：≥ 70%

### 7.2 测试文件命名

```
src/
├── services/
│   ├── matching-engine.ts
│   └── matching-engine.test.ts    # 单元测试
tests/
├── integration/
│   └── matching-binding.test.ts   # 集成测试
└── e2e/
    └── user-workflow.test.ts      # E2E测试
```

### 7.3 Mock 规范

```typescript
// ✅ 正确：Mock Core 路由
import { vi } from 'vitest';

vi.mock('@chips/core', () => ({
  Core: {
    request: vi.fn()
  }
}));

// 在测试中设置 Mock 返回值
Core.request.mockResolvedValue({
  success: true,
  data: { mimeType: 'video/mp4' }
});

// ❌ 错误：不 Mock 外部依赖
// 直接调用真实的 Core，会导致测试依赖外部系统
```

### 7.4 测试用例规范

```typescript
describe('MatchingEngine', () => {
  let engine: MatchingEngine;
  
  beforeEach(() => {
    engine = new MatchingEngine();
  });
  
  describe('analyze', () => {
    it('should return matching result when cards and files provided', async () => {
      // Given
      const cards = createMockCards(10);
      const files = createMockFiles(10);
      
      // When
      const result = await engine.analyze(cards, files);
      
      // Then
      expect(result).toBeDefined();
      expect(result.bindings.length).toBeGreaterThan(0);
    });
    
    it('should throw error when no cards provided', async () => {
      // Given
      const cards: Card[] = [];
      const files = createMockFiles(10);
      
      // When & Then
      await expect(engine.analyze(cards, files))
        .rejects
        .toThrow(FileConnectorError);
    });
  });
});
```

---

## 8. Git 规范

### 8.1 提交消息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具

**Scope 范围**：
- `matching`: 匹配引擎
- `binding`: 绑定执行
- `ui`: 用户界面
- `algo`: 算法
- `test`: 测试

**示例**：
```
feat(matching): 添加关键词匹配算法

- 实现中文分词
- 实现英文分词
- 计算相似度

Closes #123
```

### 8.2 分支管理

```
main                  # 主分支
├── develop           # 开发分支
│   ├── feature/matching-algorithm    # 功能分支
│   ├── feature/ai-integration        # 功能分支
│   └── fix/disk-space-check          # 修复分支
└── release/1.0.0     # 发布分支
```

---

## 9. 代码审查清单

### 9.1 必检项

- [ ] 遵循中心路由原则（所有通信通过 Core）
- [ ] 无硬编码文本（所有文本使用 t() 函数）
- [ ] 使用公共基础层模块（FileIdentifier、ConfigManager 等）
- [ ] 错误处理完整（try-catch，错误日志）
- [ ] 测试覆盖率达标（≥ 80%）
- [ ] 代码格式化（Prettier）
- [ ] 类型定义完整（TypeScript）
- [ ] 文档已更新

### 9.2 性能检查

- [ ] 并发数限制（最大 5 个）
- [ ] 使用缓存（匹配结果、文件列表）
- [ ] 防抖节流（高频操作）
- [ ] 进度报告（长时间操作）

### 9.3 安全检查

- [ ] 输入验证（文件路径、卡片 ID）
- [ ] 路径安全（防止路径遍历）
- [ ] 权限检查（文件读写权限）
- [ ] API 密钥加密存储

---

## 10. 开发流程

### 10.1 功能开发流程

1. **创建功能分支**
   ```bash
   git checkout -b feature/my-feature
   ```

2. **编写代码和测试**
   - 遵循本规范
   - 测试覆盖率 ≥ 80%

3. **自测通过**
   ```bash
   npm run test
   npm run lint
   npm run build
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat(scope): description"
   ```

5. **推送并创建 PR**
   ```bash
   git push origin feature/my-feature
   # 在 GitHub/GitLab 创建 Pull Request
   ```

6. **代码审查**
   - 至少一人 approval
   - 所有检查项通过

7. **合并到 develop**

### 10.2 Bug 修复流程

1. **创建修复分支**
   ```bash
   git checkout -b fix/bug-description
   ```

2. **编写测试重现 bug**
   ```typescript
   it('should handle edge case XYZ', () => {
     // 重现 bug 的测试
   });
   ```

3. **修复 bug**

4. **测试验证**
   ```bash
   npm run test
   ```

5. **提交和推送**

6. **快速审查和合并**

---

## 11. 违规处理

### 11.1 严重违规（禁止合并）

以下行为将**禁止合并代码**：
- ✗ 违反中心路由原则（直接调用其他模块）
- ✗ 包含硬编码文本
- ✗ 未使用公共基础层模块（自己实现文件识别等）
- ✗ 没有错误处理的关键操作
- ✗ 测试覆盖率不达标

### 11.2 警告处理（需要修改）

以下行为需要**修改后才能合并**：
- ⚠ 命名不规范
- ⚠ 注释缺失
- ⚠ 日志不完整
- ⚠ 性能问题（超过指标）

---

## 12. 参考资源

### 12.1 生态规范

- [生态通用开发规范](../生态共用/08-开发规范总则.md)
- [薯片协议规范](../生态共用/01-薯片协议规范.md)
- [多语言系统规范](../生态共用/11-多语言系统规范.md)

### 12.2 项目文档

- [架构设计](./技术文档/01-架构设计.md)
- [需求文档](./需求文档/)
- [技术文档](./技术文档/)

---

## 附录：开发工具配置

### A.1 VSCode 配置

`.vscode/settings.json`:
```json
{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "[vue]": {
    "editor.defaultFormatter": "Vue.volar"
  }
}
```

### A.2 ESLint 配置

`.eslintrc.js`:
```javascript
module.exports = {
  extends: [
    '@chips/eslint-config',
    'plugin:vue/vue3-recommended'
  ],
  rules: {
    // 强制使用 t() 函数
    'no-literal-strings': 'error',
    // 禁止直接导入其他模块
    'no-restricted-imports': ['error', {
      patterns: ['@chips/foundation/*', '../*/']
    }]
  }
};
```

### A.3 Prettier 配置

`.prettierrc.js`:
```javascript
module.exports = {
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'es5',
  printWidth: 80
};
```

---

**文档维护者**: 薯片生态核心团队  
**最后更新**: 2026-01-31  
**文档状态**: ✅ 完整
